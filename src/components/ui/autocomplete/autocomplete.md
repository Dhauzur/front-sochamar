# Autocomplete

## props
```
items = Array with the follow data [{ search: 'text to search', data: 'example id, item...'}...]
selected= Function, this receive the item selected example: functionExample(selected) { arrayItemsSelected.push(selected) }
placeholder= text of the input, optional.
```